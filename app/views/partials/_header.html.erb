<nav class="navbar navbar-default" role="navigation", id="navbar">
<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, class: "alert alert-info" %>
<% end %>
  <div id="logo">
    <h3> <%= link_to "MONITOR MY AIR", home_index_path, method: 'get' %> </h3>
  </div>
  <div id="account">
      <% if current_user %>
      <%= link_to "Logout", logout_path, id: "logout", method: 'delete' %>
      <% else %>
      <%= link_to "Login With Twitter", login_path, id: "login" %>
    <% end %>
  </div>
   <div>
      <% if current_user %>
        <div class="btn-group">
          <button type="button" class="btn btn-default dropdown-toggle glyphicon glyphicon-user btn-sm" data-toggle="dropdown" id="drop-down">
            You <span class="caret"></span>
          </button>
          <ul class="dropdown-menu" role="menu">
            <li> <%=link_to 'My Notifications', notifications_path %></li>
            <li><%= link_to 'My Historical Data Search', reports_path%></li>
          </ul>
        </div>
      <% end %>
    </div>
</nav>
